i3blocks
	make fn keys work with brightness and volume

emacs
	configure emacs for c/c++, java, web technologies

sublime text
	download sublime for a goto editor


* use symlinks to link all files in the cloned repo. no need to copy individual directories to $HOME
